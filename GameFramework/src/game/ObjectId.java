/*
	자바에서 지원되는 객체 중, 상수만을 놓아놓은 집합을 가리켜 enum 객체라 한다.
	enum 객체는 상수를 모아놓기 때문에 이 상수값들 사이에 index가 존재하며 배열처럼 사용할 수도 있다.
	
*/

package game;

public enum ObjectId {	
	Player, Enemey, Block, Bullet, Item			//이 자체를 상수로 만든다. 배열처럼도 사용가능
	
}
